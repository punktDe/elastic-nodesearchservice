# This search strategy does a

PunktDe:
  Elastic:
    NodeSearchService:
      logRequests: true
      searchStrategies:
        titlePrefix:
          condition: '${true}'
          request:
            query:
              bool:
                filter:
                  bool:
                    minimum_should_match: 1
                    should:
                      - multi_match:
                          query: ARGUMENT_TERM
                          type: bool_prefix
                          fields: ['title']
                    must:
                      - terms:
                          __typeAndSupertypes: ARGUMENT_SEARCHNODETYPES
                      - term:
                          __parentPath: ARGUMENT_STARTINGPOINT
                    must_not:
                      - term:
                          _hidden: true
            _source:
              - __path
            size: 20

